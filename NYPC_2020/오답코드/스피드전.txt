a = int(input())
for i in range(a):
	b = [int(l) for l in input().split()]
	e = b[:4]
	f = b[4:]
	if sum(e)>sum(f) and max(f)>max(e):
		print("S")
	elif sum(f)>sum(e) and max(e)>max(f):
		print("I")
	else:
		print("R")
